# 安全与隐私政策

## 报告漏洞

请使用 GitHub 的 Private Vulnerability Reporting 私下报告漏洞。不要创建公开 Issue，也不要附真实病人信息、导出数据、访问令牌或可用于登录第三方平台的内容。

## 数据边界

- 应用没有后端账户、API 或云数据库。病人、待办和设置保存在当前浏览器的 IndexedDB。
- Vercel 托管静态代码，不应接收业务数据。Service Worker 只处理同源 GET 请求并缓存应用资源。
- “只在本机保存”不等于“已加密”：能使用同一浏览器配置文件或设备账户的人，可能访问这些数据。
- 导出的 JSON 文件可能含敏感健康信息，必须按机构制度加密、传输、保存和销毁。

## 使用要求

- 仅在受管理、启用磁盘加密和屏幕锁的设备上使用；共享设备用后清理浏览器数据。
- 不把真实数据用于公开 Demo、测试、截图、Issue、PR 或 CI。
- 上线前由所在机构确认隐私、数据驻留、留存和医疗软件合规要求。
- 本工具不替代正式病历、医嘱系统或双人核对流程。

## 凭据管理

- GitHub/Vercel 令牌只放在系统密钥库、CI Secrets 或受保护环境变量中。
- 使用标准变量名 `GH_TOKEN` 和 `VERCEL_TOKEN`；禁止把令牌写入 Git remote URL、脚本、命令参数、日志或文档。
- 使用最小权限、明确到期时间并定期轮换。发现泄露时先撤销令牌，再清理配置和历史。

## 支持范围

当前仅维护 `main` 对应的最新生产版本。依赖升级、浏览器安全更新和令牌轮换至少每月检查一次；高危依赖或凭据事件立即处理。
