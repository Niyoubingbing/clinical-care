# 临床病人管理助手 — 产品需求文档（PRD）

> **版本**：v3.0（全量重写）
> **实现基线**：代码版本 v2.17.2（`package.json` / `public/version.json` / `public/sw.js` 三处构建时同步）
> **重写日期**：2026-08-01
> **最近核对**：2026-08-17（实现仍为 v2.17.2；架构、治理与运维分别见 `docs/ARCHITECTURE.md`、`docs/GOVERNANCE.md`、`docs/OPERATIONS.md`）
> **文档性质**：本文档以**源码为唯一实现基线**，逐功能核对 `app/`、`components/`、`lib/`、`tests/` 后重写，描述「系统现在实际做什么」。凡与旧版 PRD 冲突之处，以本文档为准。
> **范围约定**：本文档**不定义 UI 视觉设计**（配色、字体、阴影、圆角、布局像素等）；仅保留承载功能语义的交互行为（手势、阈值、状态、文案口径）。视觉规范另行维护。

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户与核心工作流](#2-用户与核心工作流)
3. [信息架构与路由](#3-信息架构与路由)
4. [功能需求](#4-功能需求)
5. [数据模型与默认值](#5-数据模型与默认值)
6. [PWA 与离线架构](#6-pwa-与离线架构)
7. [全局基础设施与约定](#7-全局基础设施与约定)
8. [测试与质量保障](#8-测试与质量保障)
9. [已知边界与行为备注](#9-已知边界与行为备注)
10. [附录：关键常量速查](#10-附录关键常量速查)

---

## 1. 产品概述

### 1.1 产品定位

面向临床医生（当前骨科，可扩展至外科）的**移动端优先、离线优先**的病人查房与待办管理 PWA。数据完全存储于用户浏览器 IndexedDB，**无后端、无账号、无服务端鉴权**；一次安装，永久本地可用。

### 1.2 核心价值

| 价值 | 落地功能 |
| --- | --- |
| 移动优先 | 底部三 Tab 导航、手势操作（左滑完成、长按菜单）、44×44 触摸目标、刘海屏安全区适配 |
| 秒开 | 全静态导出（`output: "export"`）+ SW 全量预缓存，页面与资源 cache-first |
| 智能提醒 | 逾期/今日待办/需换药/需查血四维汇总提醒条、导航角标、换药待办自动补齐 |
| 高效批量操作 | 查房前粘贴花名册批量导入（按姓名匹配新增/更新/删除），一键导出/导入 JSON 备份 |

### 1.3 产品边界

- 单机单用户；无多设备同步（备份靠手动导出/导入 JSON）。
- 不做浏览器通知推送（提醒均为应用内视觉提醒）。
- 无 `/patient/{id}` 深链接：病人详情为静态壳页，id 经 sessionStorage 传递（见 §3.2）。
- 数据不发送到任何外部域名；`fetch` 仅限同源（`/version.json`、`/sw.js` 及静态资源）。

### 1.4 核心术语

| 术语 | 定义 |
| --- | --- |
| 病房块（room block） | 查房顺序配置中的普通病床分组，含 `ward`（病区）与床号数组 |
| 真实加床块（extra block） | 查房顺序配置中的加床分组（如 `309WJ04`），块内床为「真实加床」 |
| 病房床（real） | 床号出现在任一病房块中的床 |
| 真实加床（extra-real） | 床号出现在真实加床块中的床 |
| 虚拟床（virtual） | 不在任何查房块中、或被「强制虚拟名单」命中的床；首页可一键隐藏 |
| 基础床号（bedBase） | 床号末两位数字（如 `309W41` → 41），解析字段，用于展示与基础规则匹配 |
| 完整床号 | 数字开头且能被床号解析模板匹配的床号（如 `309W41`；`J04` 这类字母开头不算） |
| 通用待办 | `patientId = null` 的待办，不归属任何病人（交班记录、写病历等） |
| 快捷待办 | 设置页维护的预置待办按钮，在病人详情页一键添加 |
| 换药计划 | `DressingSchedule { earlyInterval, laterInterval, maxDay }`，全局默认 + 病人级覆盖 |

---

## 2. 用户与核心工作流

### 2.1 目标用户

- 骨科/外科住院医师：每日查房、记录待办、跟踪换药与查血。
- 规培/进修医生：快速熟悉病房床位与病人分布。

### 2.2 每日工作流

1. **查房前**：粘贴当日花名册（每行「床号 姓名 诊断」）→ 预览差异 → 确认批量导入，病人列表与查房顺序即刻就绪。
2. **查房中**：首页按查房顺序逐床查看；点卡片进详情记录待办（支持「明天换药」「周三复查」自然语言时间）；左滑快速完成待办；提醒条实时提示逾期/今日/换药/查血。
3. **查房后**：展开「每日小结」查看当日已完成工作，一键复制文本或导出 JSON 用于交班。

### 2.3 备份工作流

设置页 →「导出数据」下载全量 JSON → 换机/重装后「导入数据」→ 确认框显示待导入计数 → 全量替换恢复。导入有严格校验红线（§4.13.3）。

### 2.4 全生命周期

- **病人**：添加（手动/批量）→ 查房/编辑 → 删除（级联删待办，toast 可撤销）。
- **待办**：创建（自动/手动/快捷）→ 完成（toggle 写 `completedAt`）→ 取消完成（清 `completedAt`）→ 删除（toast 可撤销）；每日小结按 `completedAt` 归属自然日统计。

---

## 3. 信息架构与路由

### 3.1 路由清单

| 路由 | 页面 | 说明 |
| --- | --- | --- |
| `/` | 查房首页 | 病人列表（查房顺序）、提醒条、每日小结、筛选与开关 |
| `/patient` | 病人详情 | **静态壳页**，病人 id 经 `sessionStorage["cc:pid"]` 传递（见 §3.2） |
| `/todos` | 待办页 | 全局待办视图，支持 `?filter=pending\|completed\|today\|overdue` |
| `/settings` | 设置首页 | 主题、四个子设置入口、换药规则、数据管理、版本与更新 |
| `/settings/rounding` | 查房顺序 | 块编辑、三态规则、拖拽排序、配置导入导出 |
| `/settings/bed-recognition` | 床号识别 | 床型总览、查房列表管理、解析模板、强制虚拟名单 |
| `/settings/quick-todos` | 快捷待办 | 快捷待办按钮的增删与拖拽排序 |
| `/settings/groups` | 分组管理 | 自定义查房分组的增删改色、行内重命名、拖拽排序 |

底部导航三个一级 Tab：**查房**（`/`）、**待办**（`/todos`）、**设置**（`/settings`）；`/patient` 路径高亮「查房」Tab。待办 Tab 有角标（口径见 §4.7.4）。

### 3.2 静态壳与 sessionStorage 约定（重要架构决策）

- **不存在 `patient/[id]` 动态路由**。详情页是真正的预渲染静态文件，SW 在 install 阶段即可预缓存，**离线打开任意病人（含断网后新导入的）都稳定**。
- 跳转约定：列表点击卡片 → 写 `sessionStorage["homeScroll"]`（首页滚动位置）与 `sessionStorage["cc:pid"]`（病人 id）→ `router.push("/patient")`。
- 详情页挂载后读取 `cc:pid`；读取前 `return null` 避免水合不一致；无 `cc:pid` 时显示「未选择病人」空态而非报错。
- 返回首页时等数据加载完成且双 `requestAnimationFrame` 布局稳定后恢复滚动位置（仅恢复一次，用后删除 key）。
- `vercel.json` 含 rewrite：`/patient/:path* → /patient.html`，兼容任意 `/patient/...` 子路径。

---

## 4. 功能需求

### 4.1 病人管理

#### 4.1.1 添加病人

入口：首页「添加病人」→ 底部弹层，内含双 Tab「手动添加 / 批量导入」，默认「手动添加」；Tab 选择与批量导入文本在弹层关闭后保留（组件不卸载）。

手动添加表单字段：床号（必填）、姓名（必填）、诊断（必填）、分组（可选，文本输入 + 自定义分组快捷 chips + 取色器）、手术日期（DatePicker）、查血日（自由文本，如「周一 周三」）、换药计划开关（默认关 = 继承全局规则，见 §4.8.6）。

新增提交规则：

1. 床号/姓名/诊断任一为空 → toast「床号、姓名、诊断为必填项」，不提交。
2. **床号查重**：库内已有同床号 → toast「床号 X 已被 姓名 使用」，不提交。
3. 换药计划开关开启时，仅当三值皆为整数 ≥1 且 `maxDay > earlyInterval` 才写入病人级计划；否则不写（继承全局默认）。
4. 创建时计算并持久化：`parseBed` 产出 `ward/bedBase/specialType`（展示字段），`computeBedType` 产出 `bedType`（权威床型，见 §4.9.1）。
5. 成功 toast「病人已添加」并关闭弹层；`saving` 状态防重复提交。

#### 4.1.2 编辑病人（自动保存）

入口：病人详情页右上角编辑按钮（**首页操作菜单的同名入口存在已确认缺陷，见 §9.2**）。

编辑模式**无保存按钮**，规则如下：

- **基线快照 + 增量 diff**：载入时对 11 个字段建快照；任何变化后 **400ms 防抖**自动落库，输入框 onBlur 也立即触发（双保险）。
- 必填字段（床号/姓名/诊断）被清空 → **不覆盖数据库**，字段下方内联红字「xx为必填项，已保留原值」。
- 床号修改：查重（排除自身）→ 重复则内联「床号 X 已被 姓名 使用，已保留原值」；合法则写入新床号并重算 `ward/bedBase/specialType` 与 `bedType`（`computeBedType`，查房顺序口径）。
- 分组清空即删除分组（`group` 置 undefined）；分组色、手术日期、查血日变化即写。
- 换药计划仅在开关**开启**时校验并写入（整数 ≥1 且 `maxDay > earlyInterval`）；非法 → 内联「换药间隔需为整数≥1，且截止>前期间隔，本次未保存」。**关闭开关不删除已有病人级计划**（行为备注见 §9.1）。
- 无任何改动时不写库不提示；保存成功显示「已自动保存」1.5 秒后淡出。编辑模式错误**只走内联红字，不弹 toast**。

#### 4.1.3 手术日期 DatePicker

- 月历网格弹层（复用底部弹层基座），标题随翻月变化；周表头「日一二三四五六」（周日起）。
- 每次打开视图重置到「当前选中日期」或（未选时）「今天」。
- 点日期即选中并关闭；底部「今天」（选今天并关闭）、「清除」（清空并关闭）。

#### 4.1.4 删除病人

- 入口：首页长按卡片/「⋯」菜单 →「删除病人」；或详情页底部「删除病人」按钮。
- 二次确认框：首页版文案「将删除 X 及其全部待办（可在 5 秒内撤销）。」，详情页版无「5 秒」字样。**实际撤销窗口为 toast 默认时长 3 秒**（不一致项见 §9.2）。
- 删除语义：**事务内级联删除该病人全部待办**（含已完成），随后 toast「已删除 · 撤销」。
- 撤销：删除前已快照病人对象与其全部待办，点「撤销」原样写回并 toast「已恢复」。详情页删除成功后 `router.back()` 返回。

### 4.2 查房首页（`/`）

#### 4.2.1 数据来源与派生

实时订阅（`useLiveQuery`）：`patients`、`todos`、`settings`。派生计算：

- 查房排序：`resolveOrder(settings.roundingOrder, patients)`（§4.9.3）。
- 每病人状态：`patientStatus`（需换药/需查血/今日到期/逾期/术后天数，§4.7.3）。
- 每病人床型：`computeBedType`（权威）+ `parseBed.specialType`（展示标记），构造 `bedInfoMap` 传入卡片。
- 提醒条：`computeReminders`（§4.7.1）；每日小结：`buildDailySummary`（§4.14）。
- 列表过滤：`filterHomeRows`（虚拟床隐藏 + 分组筛选，§4.2.5、§4.2.6）。

#### 4.2.2 页面操作

- 「添加病人」按钮 → §4.1.1 弹层；「通用待办」按钮 → 待办创建弹层（`patientId=null`，§4.4.4）。
- **提醒条**（无提醒时不渲染）：文案按固定顺序拼接——「N 个待办已逾期，N 人需换药，今天 N 个待办到期，N 人需查血」（仅列非零项）。级别：有逾期或需换药 → danger；否则有今日到期或需查血 → warning。点击跳转：有逾期 → `/todos?filter=overdue`，否则 → `/todos?filter=today`。
- **每日小结卡**：默认折叠，头部显示「每日小结 · 换药 N · 查血 N」（无完成项时「今日暂无完成项」）；展开后可「复制」文本或「导出 JSON」（§4.14）。
- **分组筛选**：横向 chips，首项「全部」。分组来源 = **设置页 customGroups 名称 ∪ 病人身上实际出现过的 group 值**（去重），保证空分组可筛、未登记分组兼容。无分组时整个组件不渲染。
- **「列表顺序」切换**：正序/反序，写入 `settings.listDirection` 持久化（与查房顺序配置解耦；反序 = 已排序列表整体反转，刷新后保持）。
- **「虚拟床」开关**：写入 `settings.showVirtualBeds`（默认 true）。过滤语义见 §4.2.6。

#### 4.2.3 病人卡片

- **单击** → 打开详情（记录滚动位置 → 写 `cc:pid` → 跳转）。
- **长按 500ms** → 弹出操作菜单（手指抬起/滑出取消计时）；长按触发后抑制紧随的 click，防误进详情。
- **右上角「⋯」按钮** → 同操作菜单（不触发卡片点击）。
- 键盘可达：`Enter`/`Space` 打开详情。
- 展示：床号色块（标签 = `bedBlockLabel`，底色 = 分组色，字色按亮度对比自动黑/白）、姓名、分组 chip、诊断，及徽标序列：`待办 N`（pending>0）、`术后第N天/术前第N天`、`虚拟床`、`加床`/`加床·{标记}`、`需换药`、`需查血`、`已逾期`、`今日到期`。危险状态（逾期或需换药）卡片有红色左边条。
- 操作菜单两项：「编辑 / 更改分组」（**当前实现有缺陷，见 §9.2**）、红色「删除病人」（§4.1.4）。

#### 4.2.4 合并卡片（同病房整组）

- 查房排序结果中**相邻同 `groupId` 的病人合并为整组卡片**（内嵌式病人卡片，分隔线排列）；不同块之间打散。
- 整组卡片与单卡混排保持查房顺序；虚拟滚动与进入动画行为与单卡一致。

#### 4.2.5 分组筛选语义

`filterHomeRows`：选中分组为 null 不过滤；单卡要求 `patient.group === 选中组`；**整组要求组内至少一人属于选中组**即保留整组。

#### 4.2.6 虚拟床过滤语义

`showVirtualBeds = false` 时：单卡为虚拟床 → 剔除；**整组按成员逐个过滤——仅当全组皆虚拟才剔整组，部分虚拟时保留真实成员**（生成新组对象，不就地修改）。床型一律以 `computeBedType` 判定（§4.9.1）。`showVirtualBeds = true` 时全量保留，过滤不改变相对顺序。

#### 4.2.7 三态与性能

- 加载判定用「查询结果 `=== undefined`」区分**未加载**与**真空**：加载中 →「加载中…正在读取本地数据…」；无病人 →「暂无病人，点击右上角添加或批量导入」；筛选无结果 →「未找到匹配病人，试试切换分组筛选」。
- **列表虚拟化**：过滤后行数 > 50 启用窗口虚拟化（估算行高 68、overscan 10）；监听 resize 与字体加载完成后重测列表偏移，防止行高漂移。虚拟滚动时关闭卡片进入动画。

### 4.3 病人详情页（`/patient`）

#### 4.3.1 进入与三态

- id 来自 `sessionStorage["cc:pid"]`（§3.2）；未读取完成前不渲染。
- 无 id →「未选择病人，请从查房页选择一个病人查看详情」；有 id 但数据未加载/已删除 → 骨架屏。

#### 4.3.2 信息展示

- 头部：返回键（回 `/`）、标题 = 姓名、副标题 = 分组或「病人详情」、右侧编辑按钮。
- 信息卡：床号色块 +「床号 · 诊断」+ 徽标（`虚拟床`/`加床·{标记}`/`需换药`/`需查血`/`有逾期待办`），及字段：
  - 手术日期（未设置显示「—」）。
  - 术后：有手术日 →「术后第 N 天」（手术日 = 第 0 天）/「术前第 N 天」。
  - 距下次换药：`0` →「今日换药」；`N` →「距下次换药 N 天」；`null`（计划已过截止日）→「换药计划已完成(已超14天)」（**文案中 14 为硬编码**，见 §9.1）。
  - 查血日：自由文本原样显示。

#### 4.3.3 切换分组

仅当存在自定义分组时渲染。「无」+ 各分组 chips；点击即写入 `group/groupColor` 并 toast「已切换到『X』」/「已取消分组」；当前分组高亮。

#### 4.3.4 快捷操作（QuickActions）

三宫格：

| 按钮 | 行为 |
| --- | --- |
| 换药 | **查重**：已存在该病人「类型=换药 且 到期日=今天 且 未完成」的待办 → toast「今日换药待办已存在」不创建；否则创建今日换药待办，toast「换药日期已更新」 |
| 查血 | **不查重**，直接创建今日查血待办，toast「已添加查血待办」 |
| 添加待办 | 打开待办创建弹层（带病人上下文） |

#### 4.3.5 快捷待办（QuickTodoBar）

- 渲染 `settings.quickTodos` 的全部按钮（为空则整个组件不渲染）。
- 点击行为：添加文本取 `content` 非空则用之、否则用 `label`；**查重**——该病人已存在「未完成 且 内容（trim 后）相同」的待办 → toast「该待办已存在」；否则经 `parseTime` 解析日期 + `inferTodoType` 推断类型后创建，toast「待办已添加」。

#### 4.3.6 病人待办列表

- 排序：未完成在前；同状态按到期日升序（无日期兜底排最后）；再按创建时间**倒序**（新的在前）。
- 展示与交互复用 `TodoListView`（§4.4.5）：进行中平铺，已完成默认折叠（点「已完成 N」展开）。
- 单条 toggle 走 `lib` 的 `toggleTodo`（完成写 `completedAt`，取消则清除）；单条删除 → toast「已删除 · 撤销」，撤销原样写回并 toast「已恢复」。
- 详情页待办卡片**不绑定点击跳转**（与待办页行为不同）。

### 4.4 待办系统

#### 4.4.1 数据与类型

`Todo { id, content, status, createdAt, patientId?, type?, dueDate?, completedAt? }`。`patientId = null` 即通用待办。类型共 10 个枚举值（换药/查血/开术前/出院/明天出院/康复会诊/会诊/复查/开查血/其他），创建时由 `inferTodoType` 按内容关键词自动推断（规则与边界见 §4.6.3、§9.1）。

#### 4.4.2 创建入口（三处统一）

| 入口 | 上下文 |
| --- | --- |
| 首页「通用待办」 | `patientId = null` |
| 详情页「添加待办」 | 带病人 id 与姓名，标题「添加待办 · 姓名」 |
| 详情页快捷操作/快捷待办 | §4.3.4、§4.3.5 |

创建弹层（TodoFormSheet）规则：

- 打开即重置内容；输入即**实时时间识别**，下方显示「识别到时间：YYYY-MM-DD（今明后天/星期几/具体日期）」或「未识别到时间」。
- 提交时以**最终文本重新解析**（不用实时缓存值）；空内容（trim 后）→ toast「请输入待办内容」。
- 只存「内容 + 推断类型 + 解析日期」三个字段，不再区分两套表单（设计原则）。
- 成功 toast 区分「待办已添加 / 通用待办已添加」；`saving` 防重复提交。

#### 4.4.3 完成/取消完成

- `toggleTodo(id, completed)`：完成 → `status="completed"` 且 `completedAt=Date.now()`；取消 → `status="pending"` 且 `completedAt=undefined`。
- **完成换药待办不写病人 `lastDressingChange`**（v2.17 起该字段废弃，测试锁定）；「今日已换药」由「存在今日已完成的换药待办」判定（§4.8.4）。
- 注意：待办页的 toggle 是页面内联实现（行为等价），与 `lib` 的 `toggleTodo` 为两处代码（§9.1）。

#### 4.4.4 滑动手势（SwipeableTodo）

当前唯一启用模式「swipeComplete」：

- **左滑或右滑超过 56px** → 切换完成状态，松手回弹（拖拽约束 ±90px、elastic 0.08、无惯性）。
- 行内等价按钮：左侧圆圈/对勾 → 完成切换（手势的无障碍等价入口）；右侧垃圾桶 → 删除。
- 展示：内容（完成加删除线、整卡淡化）、类型徽章（「其他」不显示）、到期徽章（`已逾期 N 天`/`今天到期`/`明天到期`/`还剩 N 天`）、完成徽章。
- 组件内另保留一套「左滑露出圆形完成/删除按钮」的停靠式实现（阈值 ±50px、停靠宽度 112px），**当前无调用方**（备用，见 §9.1）。

#### 4.4.5 列表组件（TodoListView）

- 进行中按 §4.3.6 规则排序平铺；已完成**保持原顺序**，默认折叠（「已完成 N」可展开）。
- `expandCompleted=true` 时已完成直接平铺（待办页「已完成」筛选使用）。**该属性仅在组件挂载时生效**（见 §9.1）。
- 全部为空显示「暂无待办」。
- **虚拟化**：条目 > 30 启用窗口虚拟化（估算行高 56、overscan 8）并关闭逐项布局动画。

### 4.5 待办页（`/todos`）

#### 4.5.1 筛选

- 四个筛选 chip：`未完成`（默认）、`已完成`、`今天到期`、`已逾期`。
- 初始值取 URL `?filter=`，**仅四个合法值生效，其他（含旧链接 `?filter=all`）回退「未完成」**。
- 过滤口径：未完成 → `status="pending"`；已完成 → `status="completed"`；今天到期/已逾期 → pending 且 `dueLabel` 级别为 today/overdue。
- 因使用 `useSearchParams`，静态导出下整页包 Suspense（「加载中…」）。

#### 4.5.2 通用待办区（置顶）

- 永远置顶；仅当有条目通过当前筛选时渲染。
- 计数 chip 随筛选变化：「已完成」筛选下显示「N 已完成」，其余筛选显示「N 进行中」。

#### 4.5.3 病人待办区

- 按 `patientId` 聚合；**仅渲染至少有一条待办通过当前筛选的组**。
- 组间排序：按床号 `localeCompare`（numeric）数字序稳定排列。
- **孤儿待办**：病人已删但待办仍在 → 归入「未分配病人」兜底组（无床号色块）。
- 组头部：床号色块、姓名、床号、分组 chip。点击单条待办 → 写 `cc:pid` 跳详情页。
- 「已完成」筛选下已完成项平铺（`expandCompleted`）；其余筛选已完成折叠。
- 无符合筛选的待办 →「没有符合条件的待办，切换筛选或添加待办」。

### 4.6 时间解析与类型推断

#### 4.6.1 `parseTime`（中文自然语言时间）

**优先级：今明后天 > 星期 > 具体日期，返回首个最高优先级命中**；全不命中返回 null。

1. **今明后天**（子串匹配）：`今天/今日`=+0、`明天/明日/明早`=+1、`后天/后日`=+2，标签「今明后天」。
2. **星期**：`下下(星期|周|礼拜)X` = 目标星期 +14 天；`下(星期|周|礼拜)X` = +7；`(星期|周|礼拜)X` = 本周最近一个 X（今天即目标星期时 = 今天），X ∈ 日/天/一…六，标签「星期几」。
3. **具体日期**：`YYYY-M-D`（- 或 /）、`M-D`（补当年）、`M月D日`（补当年）；校验月 1–12、日 1–31 且 Date 合法（**不拦截 JS 进位**，如 2 月 31 日按 3 月 3 日计），标签「具体日期」。

#### 4.6.2 `dueLabel`（到期标签五档）

以待办 `dueDate` 与今天（0 点对齐）差值 days 分级：无日期/非法 → 「无日期」(none)；`days<0` →「已逾期 N 天」(overdue)；`=0` →「今天到期」(today)；`=1` →「明天到期」(soon)；`≤3` →「还剩 N 天」(soon)；更大 →「还剩 N 天」(future)。提醒与角标只统计 overdue/today 两档。

#### 4.6.3 `inferTodoType`（待办类型推断）

有序关键词表，**先命中先返回**：`开查血`→开查血、`查血`→查血、`换药`→换药、`开术前`→开术前、`术前`→开术前、`出院`→出院、`康复会诊`→康复会诊、`会诊`→会诊、`复查`→复查；全不命中 →「其他」。

注意（测试锁定的现状）：纯子串匹配无语义判断——「术后复查不是术前安排」会命中「术前」判为「开术前」；枚举中的「明天出院」永不产出（含「明天出院」的文本判为「出院」，但日期会正确解析为明天）。

### 4.7 提醒系统

#### 4.7.1 `computeReminders`（提醒条汇总）

统计口径：待办只数「pending 且有 dueDate」的 overdue/today；病人维度独立计算需换药（§4.8.4）、需查血（§4.7.2）。

**三级告警**：有逾期待办 或 有需换药 → **danger**；否则有今日到期 或 有需查血 → **warning**；否则 none。任一计数 > 0 即显示提醒条（文案与跳转见 §4.2.2）。

#### 4.7.2 `needsBlood`（需查血）

`bloodTestDay` 按 `/[\s、，,]+/`（空格/顿号/中英文逗号）切分，与今天的「周X」中文名（周日…周六）**精确相等**匹配。输入「周一 周三」有效；「周一上午」「星期一」永不命中。

#### 4.7.3 `patientStatus`（单病人状态）

输出七字段：需换药、需查血、今日到期、逾期（均按上述口径）、术后天数、今日是否换药日、距下次换药天数。无换药计划上下文时换药相关字段置假。`pendingTodoCount` 为该病人未完成待办数（卡片「待办 N」徽标）。

#### 4.7.4 导航角标

待办 Tab 角标 = 全库「pending 且到期级别为 today 或 overdue」的待办数（含通用待办）；>99 显示「99+」。

### 4.8 换药管理

#### 4.8.1 换药计划模型

`DressingSchedule { earlyInterval, laterInterval, maxDay }`，全局默认 `{2, 3, 14}`。**病人级覆盖优先**：`病人.dressingSchedule ?? 全局.dressingSchedule`。

#### 4.8.2 换药日序列

`dressingDays`：首换日 = `earlyInterval`，其后每次 `+= laterInterval`，直到 `> maxDay` 停止。例：`{2,3,14}` → 术后第 **2、5、8、11、14** 天。护栏：`earlyInterval < 0` 或 `laterInterval <= 0` → 空序列（`earlyInterval = 0` 合法，即手术日当天换药）。

#### 4.8.3 术后天数

`postOpDay`：手术日 = 第 0 天；术前为负；未设手术日/日期非法 → null（按本地 0 点对齐计算）。

#### 4.8.4 「需换药」判定（不依赖待办是否存在）

- `isDressingDay`（今天是换药日）五重条件：术后天数非 null ∧ 在换药日序列中 ∧ ≥0 ∧ ≤maxDay ∧ ≥earlyInterval。
- `doneToday`：存在该病人「已完成 且 类型=换药 且 到期日=今天」的待办。
- **`needsDressing = isDressingDay ∧ ¬doneToday`**——即使自动建待办未运行，「需换药」状态依然成立。
- `nextDressingDate`：第一个 ≥ 今天的换药日；全部过期（超 maxDay）→ null（详情页显示「换药计划已完成」）。

#### 4.8.5 换药待办自动补齐（幂等）

- 触发时机：应用启动（设置加载完成后）执行一次；**页面从后台切回可见**（visibilitychange）再执行；全程静默失败。
- 规则：仅对「有手术日期 且 今天是其换药日」的病人，创建 `内容=换药, 类型=换药, 到期日=今天, 未完成` 的待办。
- **幂等键**：`{patientId}|换药|{dueDate}`——同键待办**任何状态**（含已完成）存在即跳过；只建今天、不补历史日。
- 与手动「换药」快捷按钮共用同一查重口径。

#### 4.8.6 换药规则配置

- **全局**：设置首页可折叠卡片（默认展开），三个数字字段（前期间隔/后期间隔/截止天数），头部动态文案「术后第 2 天起换药，每 3 天一次，至第 14 天」，底部附示例「2 / 3 / 14 → 第 2、5、8、11、14 天」。校验：三值皆整数 ≥1 即保存（**无 maxDay > earlyInterval 校验**，与病人表单强度不同，见 §9.1）。
- **病人级**：病人表单内开关 + 三数字输入（校验更严：`maxDay > earlyInterval`），语义见 §4.1.1/§4.1.2。

### 4.9 查房顺序与床号管理

#### 4.9.1 床型系统（真实/加床/虚拟的唯一权威）

**`computeBedType` 是床型判定的唯一权威**（v2.17.2 起）。按优先级：

1. 空床号 → `virtual`（脏数据兜底，绝不静默判真）。
2. **强制虚拟名单 `virtualOverrides` 优先级最高**（完整床号精确匹配）→ `virtual`。
3. 无查房块/空配置 → `virtual`。
4. **双口径匹配**（全局开关，非逐块）：任一块含完整床号 → 整体按**完整床号精确匹配**（大小写敏感，`309w41`、`309WJ04 ` 带空格、`309W41-1` 子床号均不等同）；否则按**基础床号数值匹配**（`bedBase` 相等即命中，不校验病区——基础规则天然多病区共享）。
5. 命中块 `kind = extra`（含旧数据字面量 `extra-real`）→ `extra-real`；命中 `room` → `real`；都不命中 → `virtual`。

配套：`isVirtualBed`（= 判定结果为 virtual）、`isBedInRoundingOrder`（是否入列，**忽略强制名单**，用于设置页展示）。

**展示与判定分离**：`parseBed` 的 `bedType` 输出仅用于展示，不参与判定；其 `ward/bedBase/specialType` 为展示字段。**「符合床号格式 ≠ 真实床」**——`309W99` 能匹配模板但不在查房列表 → 虚拟床。

#### 4.9.2 床号解析模板（仅影响展示）

- 默认模板：`^(\d{3})([A-Z])([A-Z]{0,2})?(\d{2})$`，四个捕获组约定为「病区基底 / 方位 / 特殊标记 / 基础床号」（`309W01`、`309WJ04`、`309WYZ05` 均可解析）。
- 默认特殊标记：`["J", "YZ"]`；标记大写化后与名单比较（名单需存大写）。
- 解析兜底：模板不匹配时 `ward` 尝试按「前导数字+首字母」推断（如 `309w01` → `309W`）；`bedBase` 用末尾数字串兜底；**畸形模板（未闭合括号等）不抛异常**，直接返回未匹配结果。
- 床号色块标签 `bedBlockLabel`：取床号最后一组数字的末两位；无数字取前 3 字符。

#### 4.9.3 查房排序 `resolveOrder`

输入查房配置与病人列表，输出带 `groupId/groupLabel` 的有序序列：

- **口径判定**：任一块含完整床号 → 整体按默认规则（完整床号）处理。
- **默认规则**：按块顺序、块内床号顺序，以病人床号精确匹配（Map 查找）。`groupId = 块 id`。
- **基础规则**（块内为基础床号）：先取全部病区去重、按中文 locale 升序；**对每个病区依次套用同一套块模板**，按「病区相等 + bedBase 数值相等」匹配。`groupId = {块id}#{病区}`（隔离后缀）。
- **去重**：一个病人只进一个组（两块列同床号只放第一个）。
- **未入列病人**：末尾追加，按「病区（中文 locale）+ bedBase」升序，`groupId/groupLabel = null`；键全等时保持稳定序。
- 正/反序不在此处理（首页按 `listDirection` 展示层反转）。

块标签 `blockLabel`：加床块有床 →「加床 J04 J06」、无床 →「加床块」；病房块空 →「空病房块」；床号末位数字连续且多于 1 张 →「首床 – 末床」（如 `309W01 – 309W03`）；否则全列。

#### 4.9.4 规则三态与默认配置

`RoundingConfig { ruleType: "default" | "basic" | "custom", regularBedCount?, avgBedsPerRoom?, blocks }`。

- **default（默认规则）**：内置 309W 示范布局——18 个病房块（01–43 连续）+ 15 个真实加床块（J04、J06、J07、J08、J09、J10、J12、J13–J18，**无 J05、J11**，忠实映射真实病区），查房路线为 41→38→36→01→04→34→07→10→13→16→19→22→25→28→31 蛇形穿插加床。病房块 `ward` 由首床号去末尾数字推导。
- **basic（基础规则）**：仅基础床号，由「普通病床数 ÷ 平均病房床数」推导病房块（§4.9.5）。
- **custom（自定义）**：**任何块手工编辑（增删块、增删床号、床号识别页加/移床）都会把 ruleType 置为 custom**。

设置页「恢复」按钮 → 重置为 default 配置并 toast「已恢复默认规则」。

#### 4.9.5 查房顺序设置页（`/settings/rounding`）

- **三态切换**：→ default 直接重置；→ basic 时若当前是 default/任一块含完整床号/无块 → 清空块走向导，否则保留已编辑块；→ custom 仅改标记。说明文案随态变化。
- **基础规则向导**（仅 basic 态）：输入「普通病床数量」（初值 40，钳制 1–2000）与「平均单一病房床数」（初值 3，钳制 1–200）→ 生成病房块：从 01 起两位补零连续填充、每块 avg 床、**最后一块可不足**、不含加床块与病区；toast「已按 N 床 / 每房 M 床 生成病房块」。
- **块列表**：垂直拖拽排序（稳定 `block.id` 作 key，拖拽手柄启动）；每块显示类型徽标（病房块/真实加床块）、病区与「共 N 床」；床号 chips 可逐个移除；块内输入框添加床号（Enter 或点「添加」；病房块输入数字自动拼 `{ward}{两位床号}`，加床块按原文接收如 `J04`/`309WJ04`），添加后按末位数字升序规整；删除块无二次确认。
- **底部**：「添加病房块」（预填 01）、「添加真实加床块」（空块待添加床号）。
- **导入/导出折叠区**（默认收起）：「复制当前配置」→ JSON（2 空格缩进）写剪贴板，**剪贴板失败时降级**填入 textarea 提示手动复制；textarea 粘贴 JSON →「导入并应用」：顶层宽容（ruleType 非法归一为 custom）、块级严格（块非对象/beds 非字符串数组/kind 未知 → 整体失败 toast「导入失败：文本格式不正确」），合法导入自动补块 id、规整床号升序。
- **保存方式**：所有变更即时写库（无显式保存按钮）。

#### 4.9.6 床号识别设置页（`/settings/bed-recognition`）

核心规则（页内明示）：床号出现在任一「病房块」→ 病房床；出现在「真实加床块」→ 真实加床；不在任何块里 → 虚拟床；**床号为精确匹配（区分大小写）**。

- **病人床型总览**（按床号中文 locale 排序）：每病人显示解析结果（病区/基础床号/特殊标记）、归属（所属块标签 或「未在查房列表中」、可选「· 已强制虚拟」）、床型徽标。
  - 已入列 →「移出查房列表（变虚拟床）」按钮：从所有块移除该床号（ruleType → custom）。
  - 未入列 → 下拉选块（含「＋ 新建真实加床块」伪选项）+「加入」：先从所有块移除（保证只归属一个块）再加入目标块；**同时自动解除该床的强制虚拟**（避免加入后仍显示虚拟床）。
- **查房列表管理**：块的增删、块内床号增删（与查房顺序页同一数据源）；删除块 toast 提示「块内床号变为虚拟床」；页内提示「块的顺序即查房顺序，可在 设置→查房顺序 拖拽调整」。
- **床号解析折叠区**（默认收起）：编辑解析模板（正则）与特殊床标记（逗号分隔，保存时 trim + 大写化）；「保存模板」先 `new RegExp` 校验，无效 toast「正则表达式无效」；**「重新解析全部」仅回写 ward/bedBase/specialType 三个展示字段，刻意不写 bedType**（防「改模板 → 误判虚拟 → 首页隐藏」历史缺陷复发）。
- **强制虚拟床折叠区**（默认收起）：维护 `virtualOverrides` 完整床号名单（增删即时生效）；明示「强制名单优先级高于查房块匹配」。
- 页面 state 在设置加载后同步（防「打开页面输入框为空、点保存把已配置清空」）。

#### 4.9.7 配置迁移（旧数据兼容）

`migrateRoundingOrder` 支持三种旧格式：已是块模型（保留各字段）；旧 `string[]`（包成单个病房块，ruleType=custom）；旧 `Unit[]`（`extra-real` 映射为 extra 块，不识别块兜底为空病房块，ruleType=custom）；无法识别 → 回落默认配置。迁移由 `ensureSettingsMigrated` 一次性执行（§5.4）。

### 4.10 分组管理

- **用途**：维护自定义查房分组（名称 + 颜色 + 顺序），供首页筛选、详情页切换、病人表单快捷选择。
- **默认四组**：解组 `#fecaca`、勇组 `#bfdbfe`、李组 `#bbf7d0`、王组 `#fde68a`。
- **添加**：名称（Enter 提交，空名 toast「请输入分组名称」）+ 取色器 + 8 色预设色板（初始选中第 3 色 `#bbf7d0`）。
- **列表**：拖拽排序（稳定 id）；点圆形色块直接改色；**点名称进入行内编辑**（blur/Enter 提交、Escape 取消、空值回退原名）；删除带撤销（先写库再 toast「已删除 · 撤销」，撤销回写旧数组）。
- **分组色兜底**：病人未指定分组时用 `DEFAULT_GROUP_COLOR = #e2e8f0`；分组徽章底色 = 色值 + `22` 透明度，字色按亮度对比自动黑/白（阈值 0.6，支持 3 位 hex）。

### 4.11 快捷待办配置

- **默认三条**：换药（类型=换药）、查血（类型=查血）、术前准备（类型=开术前）。
- **添加**：输入标签（Enter 提交，空标签 toast「请输入标签」）→ 追加 `{ id, label, type: inferTodoType(label), content: label }`（类型自动推断）。
- **列表**：拖拽排序（旧数据无 id 时以 `qt-{序号}` 兜底作 key；迁移时补 `qt-{序号}-{label}`）；删除带撤销（同 §4.10 模式）。
- 消费方：病人详情页 QuickTodoBar（§4.3.5）。

### 4.12 批量导入（花名册）

#### 4.12.1 输入格式

每行一个病人：`床号 姓名 诊断`（空格或制表符分隔，**诊断可含空格**——第 3 列起全部拼接）。少于 3 列的行记入 skipped（原因「缺少床号/姓名/诊断（至少 3 列）」，含行号与原文）。

#### 4.12.2 匹配与预览

- **匹配键是姓名**（非床号非 id）；**名单内同名只保留最后一行**。
- 三分桶：`toAdd`（库内无此名）、`toUpdate`（库内有此名）、`toRemove`（勾选「移除未出现的病人」时，库内有而名单无的全部）。该复选框**默认勾选**，切换时若已有文本自动重跑预览。
- 修改文本后预览即刻失效（需重新预览）。
- 预览卡：`新增 X 人 · 更新 Y 人 · 删除 Z 人`；待删除名单红字列出并标注未完成待办数；skipped 黄字最多列前 5 条。

#### 4.12.3 确认与落库

- 「确认导入」在无预览时禁用；**若开启移除且待删病人中有未完成待办 → 先弹二次确认**（列出「姓名(N待办)」，文案称「级联删除其未完成待办」，**实际删除全部待办**，见 §9.2）。
- 落库（事务内）：
  - 新增：分组色用默认兜底色；`ward/bedBase/specialType` 走解析；`bedType` 走 `computeBedType`。
  - 更新：仅刷床号/诊断/解析字段与床型（**不动姓名/分组/手术日期等**）。
  - 删除：级联删除该病人全部待办。
- 完成 toast「新增 N 人、更新 N 人、删除 N 人」并关闭；失败 toast「批量导入失败，请检查内容后重试」。

### 4.13 数据导出/导入/清除

#### 4.13.1 导出

「导出数据」→ 下载 `clinical-care-YYYY-M-D.json`，内容 `{ patients, todos, exportedAt }`（全字段浅拷贝），toast「已导出数据」。

#### 4.13.2 导入（全量替换语义）

1. 选文件（JSON）；**超过 10MB 在读取前直接拒绝**（「导入文件不能超过 10MB」）。
2. 解析：非 JSON → 报错；非对象 →「文件格式不正确」；patients/todos 非数组按空处理，**两者皆空 →「文件中没有病人或待办数据」**；仅 patients 可合法。
3. 确认框显示「将覆盖当前数据：待导入 病人 N · 待办 N」。
4. **逐条校验红线（先校验后清库）**：病人须含非空 id/bedNumber/name/diagnosis 且 createdAt/updatedAt 为数字（**空诊断也拒绝**）；待办须含非空 id/content、status ∈ {pending, completed}、createdAt 为数字。**任一不合法 → 整体中止，绝不清库**。
5. 全部通过后事务内清空两表 + 批量写入（**全量替换，不是合并**）→ toast「导入完成」。

#### 4.13.3 清除所有数据

- 确认框：「将清空病人与待办，并重置查房顺序与快捷待办。此操作不可撤销。」
- 实际语义：清空 patients/todos 两表；**settings 逐字段保留**（查房顺序、列表方向、快捷待办、自定义分组、主题、床号模板、特殊标记、换药规则、虚拟床开关、强制虚拟名单——均用现值，缺失才回落默认）。强制虚拟名单与床号模板被明确视为「设置而非业务数据」。完成 toast「已清除所有数据」。

### 4.14 每日小结

- **统计口径**：仅「已完成 且 `completedAt` 属于今天」的待办（按完成时间归属自然日，非到期日）。
- 计数：类型=换药/查血 分别计数（其他类型不计数但仍列明细）。
- **文本格式**：
  ```
  {今天日期} 工作小结
  换药：N 项
  查血：N 项

  {床号} {姓名}：给该病人{内容1}，给该病人{内容2}
  通用：{内容1}，{内容2}
  ```
  病人行顺序 = 完成顺序；「通用」行收容 `patientId=null` 及病人已删的孤儿待办。
- 操作：「复制」→ 写剪贴板 + toast「小结已复制」；「导出 JSON」→ 下载 `{YYYY-MM-DD}-summary.json`（字段：`date, dressing, bloodTest, patients, general`）。

### 4.15 主题

- 三态：`浅色 / 深色 / 系统`；**默认浅色**。
- **一次性迁移**：存量 `theme="system"` 强制翻为 `"light"`（消除暗色设备被迫进暗色的体感问题；已显式选过 dark/light 的用户不受影响）。
- **首屏防闪**：`<head>` 内联同步脚本在渲染前按系统偏好给 `<html>` 加 `dark` 类并同步 `color-scheme`；设置加载后由 Providers 按 `settings.theme` 校准；`system` 态监听系统偏好变化动态跟随。

### 4.16 PWA 更新机制

- **更新检测**：注册 SW 后立即 `update()` 一次，之后每 60 秒轮询 + 页面回可见时再查；`/version.json` 始终 `no-cache` 拉取且不参与预缓存。
- **可更新判定**（任一）：注册时已有 waiting worker；发现新 worker 安装完成且存在旧 controller；手动「检查更新」发现有 waiting 或远程版本 ≠ 本地版本。
- **呈现**：底部「发现新版本」横幅（可「稍后更新」关闭，仅本次会话）+ 设置页「关于应用」提示卡（含版本号与说明「旧版本仍可正常运行，本地数据全部保留」）。
- **激活**：**仅由用户点击触发**——向 waiting worker 发 `SKIP_WAITING`，新 SW 接管（controllerchange）后整页刷新；无 waiting 的兜底为直接刷新。**绝不自动激活**。
- **状态机**：`idle / checking / latest / available / error`；`latest` 与 `error` 在 3.5 秒后自动回弹 `idle`（恢复按钮可点）。
- **dev 环境保护**：非 production 默认不注册 SW，且主动注销残留注册；联调开关 `localStorage["cc-dev-sw"]="1"` 或 URL `?sw=1`。

---

## 5. 数据模型与默认值

### 5.1 Dexie Schema

数据库 `ClinicalDB`，版本 1（无 upgrade 链，迁移在应用层完成）：

| 表 | 索引 | 说明 |
| --- | --- | --- |
| `patients` | `id, bedNumber, name` | 主键 `id`（string，`crypto.randomUUID()`，兜底时间戳+随机串） |
| `todos` | `id, patientId, status, dueDate` | `patientId = null` 为通用待办 |
| `settings` | `id` | 单行 `id = 1` |

首次建库（populate）种入默认设置。

### 5.2 Patient 字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id / bedNumber / name / diagnosis / createdAt / updatedAt` | ✓ | 导入校验同样要求 |
| `group / groupColor` | – | 分组与分组色（默认 `#e2e8f0`） |
| `surgeryDate` | – | 手术日期 YYYY-MM-DD |
| `dressingSchedule` | – | 病人级换药计划（覆盖全局） |
| `bloodTestDay` | – | 查血日自由文本（如「周一 周三」） |
| `ward / room / bedBase / specialType` | – | 床号解析展示字段 |
| `bedType` | – | 持久化床型（卡片优先用实时计算值，此字段兜底） |
| `dressingFrequency / lastDressingChange` | – | **已废弃**，仅为兼容旧导入/导出保留，不参与计算 |

### 5.3 Settings 字段与默认值

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `roundingOrder` | 内置 309W 默认配置（§4.9.4） | 查房顺序块模型 |
| `listDirection` | `"forward"` | 列表正/反序 |
| `quickTodos` | 换药/查血/术前准备 三条 | §4.11 |
| `customGroups` | 解组/勇组/李组/王组（四色） | §4.10 |
| `theme` | `"light"` | §4.15 |
| `bedTemplate` | `^(\d{3})([A-Z])([A-Z]{0,2})?(\d{2})$` | 床号解析模板（仅展示） |
| `specialMarks` | `["J", "YZ"]` | 特殊床标记 |
| `dressingSchedule` | `{ earlyInterval: 2, laterInterval: 3, maxDay: 14 }` | 全局换药规则 |
| `showVirtualBeds` | `true` | 虚拟床显隐 |
| `virtualOverrides` | `[]` | 强制虚拟名单（完整床号精确匹配） |

### 5.4 迁移与数据安全机制

- **`getSettings` 纯读**：只返回迁移后视图，不在查询内写库（消除「读里写」订阅回环）。
- **`ensureSettingsMigrated` 一次性执行**（模块级 flag），四个触发条件任一满足即写回：查房顺序非块模型 → 迁移；快捷待办缺 id → 补；`theme="system"` → 翻 `"light"`；换药规则/虚拟床开关缺失 → 回补默认。
- **`updateSettings`**：事务内读-改-写（防两页并发互相覆盖）。
- **`deletePatient`**：事务内先删该病人全部待办再删病人。
- **新增/更新病人**：自动刷 `updatedAt`；创建时计算解析字段与床型。

### 5.5 本地存储约定

| 存储 | 键 | 用途 |
| --- | --- | --- |
| sessionStorage | `cc:pid` | 当前详情页病人 id |
| sessionStorage | `homeScroll` | 首页滚动位置（返回恢复，用后即删） |
| localStorage | `cc-dev-sw` | dev 环境强制注册 SW 的联调开关 |
| IndexedDB | `ClinicalDB` | 全部业务数据与设置 |

`localStorage` 仅存开关，不存临床数据。

---

## 6. PWA 与离线架构

### 6.1 技术约束

- `next.config.mjs`：`output: "export"` 全静态导出（禁 API Route、禁服务端渲染、图片 `unoptimized`）。
- `manifest.json`：`name` 临床病人管理助手 / `short_name` 临床助手、`display: standalone`、`orientation: portrait`、`start_url/scope: /`、SVG 图标（兼作 apple-touch-icon）。
- 字体：`@fontsource-variable/noto-sans-sc` 本地打包（无运行时外部字体请求）。

### 6.2 Service Worker 缓存策略（手写 `public/sw.js`）

- **版本化缓存名**：`clinical-care-v{APP_VERSION}`（构建时同步版本）；激活时删除所有旧版本缓存（病人壳缓存键跨版本保留）。
- **预缓存**：install 阶段 `cache.addAll` 全量清单（构建脚本注入）；**不调用 skipWaiting**——新版本进入 waiting，旧版本继续完整运行。
- **页面导航请求**：预缓存命中即返回 → 未命中走网络并回写缓存（`/patient` 路由同时写入跨版本保留的「病人壳」缓存键）→ 网络失败兜底：病人路由先回退病人壳，再回退已缓存首页壳，最后 `Response.error()`。
- **静态资源**：cache-first；未命中走网络，200 且 basic/cors 类型时写入运行时缓存。
- **激活唯一途径**：收到 `{ type: "SKIP_WAITING" }` 消息（用户点更新）→ `skipWaiting()`。

### 6.3 构建与部署管线

- `prebuild`：`scripts/sync-version.mjs` 将 package.json 版本同步到 `public/version.json` 与 `sw.js`（保证每次部署 sw.js 内容变化，浏览器才能检测到新 SW）。
- `build`：`next build` 后执行 `scripts/gen-sw-precache.mjs`——扫描 `out/` 生成全量预缓存清单注入 `sw.js` 占位符（排除根级 `sw.js`、`version.json`、`404.html`；注入前轮询等待 `out/sw.js` 就绪防竞态）。
- `vercel.json`：`buildCommand: npm run build`、`outputDirectory: out`；**恰好 4 条 rewrites**（测试锁定）：`/todos → /todos.html`、`/settings → /settings.html`、`/settings/:path* → /settings/:path*.html`、`/patient/:path* → /patient.html`。
- 沙箱部署：`deploy-push.mjs` 经 GitHub Git Data API 推送 main，Vercel 关联仓库自动构建。
- 生产地址：`https://clinical-care.vercel.app`。

---

## 7. 全局基础设施与约定

### 7.1 Providers（全局上下文）

- 暴露 `useApp()` → `{ toast, update }`。
- **Toast**：顶部堆叠可多条；默认 3 秒自动消失（`duration ≤ 0` 可常驻，当前未使用）；action 按钮（如「撤销」）点击 = 执行动作 + 立即关闭该条。
- 挂载时执行一次性设置迁移（§5.4）与换药待办自动补齐（§4.8.5）。
- `MotionConfig reducedMotion="user"`：全站动效尊重系统「减弱动态效果」偏好。

### 7.2 弹层基座

- **BottomSheet**：底部弹层（遮罩点击关闭、ESC 关闭、右上角 X 关闭；顶部抓手仅装饰，**无拖拽关闭手势**）；被添加病人、编辑病人、待办表单、DatePicker、操作菜单复用。
- **ConfirmDialog**：确认框（点遮罩 = 取消；确认按钮 danger 态红色）；层级高于 BottomSheet，可在弹层之上再弹确认。
- **EmptyState**：通用空态卡片（标题 + 提示 + 可选操作区）。
- **SubpageHeader**：子页头部（返回键默认回 `/settings`，详情页覆盖为 `/`）。

### 7.3 交互与可访问性约定

- 所有滑动手势均有**非手势等价按钮**（完成/删除行内按钮）。
- 交互元素配 `aria-label`；触摸目标 ≥ 44×44；`focus-visible` 有焦点框。
- 长按 500ms 触发菜单并抑制误触 click；全局输入字号 16px 防 iOS 自动缩放；适配刘海屏安全区。
- 动效只用 transform/opacity（禁整页 scale 防掉帧）；页面过渡 0.3s 统一缓动曲线。

### 7.4 性能约定

- 大列表虚拟化双阈值：首页 > 50 行、待办 > 30 条（`@tanstack/react-virtual` 窗口虚拟化）。
- 列表项 `React.memo` + 自定义比较（勾选待办仅重渲染所属病人卡）。
- `useLiveQuery` 查询命中 Dexie 索引；床型/解析等派生值 memo 化。
- 层级（z-index）约定：更新横幅 > Toast > 确认框 > 弹层 > 遮罩 > 底部导航 > 背景。

---

## 8. 测试与质量保障

### 8.1 单元/集成测试（Vitest，20 个测试文件）

环境：node + `fake-indexeddb`（内存 IndexedDB，真实落库）；`theme-apply` 单测用 jsdom 渲染真实 Providers。`run-vitest.sh` 设 `NODE_OPTIONS=--use-system-ca` 后执行 `vitest run`。

按被测模块锁定的关键行为：

| 测试 | 锁定的行为 |
| --- | --- |
| `rounding` | 空块按病区+床号升序追加；完整床号精确匹配；基础规则病区隔离与中文排序；同床号跨块不重复放置；未匹配稳定序 |
| `bed-parser` | 模板四组解析；J/YZ/自定义标记；未匹配一律 virtual；畸形模板不抛；病区推断；非 4 组自定义模板回归 |
| `bed-type` | 块归属决定床型；overrides 最高优先；大小写/空格/子床号不等同；旧 `extra-real` 字面量兼容；基础规则按 bedBase |
| `virtual-bed`（18 断言） | 隐藏语义（单卡剔除/整组按成员过滤）；符合模板但不在列表仍剔除；overrides 不连坐；与分组筛选组合；settings 未加载全判虚拟 |
| `time-parser` / `infer-todo-type` | 五档到期标签；三类时间解析；关键词顺序优先级（含「术后复查不是术前安排」→开术前 的反直觉锁定） |
| `quick-todo-date` | 「明天出院」→ 日期明天 + 类型出院（回归点） |
| `dressing` | 换药日序列与护栏；术后天数；isDressingDay/doneToday；幂等键格式；自动补齐（幂等、只建今日、未来手术日与超 maxDay 不建） |
| `rounding-edit` | 配置导入升序规整；四类畸形输入返回 null |
| `export-import` | 10MB 上限；解析报错口径；**先校验后清库红线**（畸形数据不动作，原数据纹丝不动） |
| `batch-import` | 按姓名匹配三桶；同名保留最后一行；removeAbsent 语义 |
| `db-clear-data` | 清除数据保留 customGroups/listDirection 等全部设置字段 |
| `theme-migration` / `theme-apply` | system→light 一次性迁移；三态主题类与 colorScheme 套用 |
| `toggle-dressing` | 完成换药不写 `lastDressingChange`；doneToday/needsDressing 语义 |
| `contrast` | 对比字色阈值与 3 位 hex；非法输入兜底 |
| `surface-css` | 源码级约束：颜色 token 必须包 `rgb()` |
| `vercel-404-fix` | vercel.json 恰好 4 条 rewrites；SW 导航不透传服务端 404 |
| `qa2-independent-verify` / `qa2-parity` | 床型判定与查房入列等价性（三场景零 diff）；基础规则重复 bedBase 已知分裂仅记录 |

### 8.2 E2E 测试（Playwright）

- 配置：视口 390×844（iPhone 12/13，移动优先）；本地静态服务器托管 `out/` 产物（与线上一致），内置路由映射与路径穿越防护；所有用例拦截 `sw.js` 返 404 隔离 SW 干扰。
- `smoke`：7 个页面逐个加载，导航挂载即过，控制台错误过滤已知噪声后软记录。
- `flow`：添加病人全流程、通用待办实时时间识别、批量导入预览与确认、**正/反序切换刷新后持久化**、合并卡片结构、字体与导航尺寸。
- `perf`：首屏 TTFB/DCL/load/FCP 写入报告（只记录不设阈值）。
- `a11y`：4 个页面跑 axe-core `wcag2a + wcag2aa`，**违规必须为空（硬断言）**。

### 8.3 代码质量红线（CODE_REVIEW 摘要）

1. **先完整校验、再清空写入**：所有导入/重置路径禁止「清空后写残缺数据」。
2. 床号模板是用户可编辑的正则，**写入前必须校验合法性**（捕获 SyntaxError）。
3. **querier（useLiveQuery 回调）内禁止写库**（防订阅回环）；迁移写回用一次性 flag。
4. 拖拽/排序必须用**稳定唯一 id** 作 key/value（禁索引/对象引用）。
5. 跨表写操作必须包 Dexie 事务。
6. 禁止 `dangerouslySetInnerHTML` 渲染用户数据；导入 JSON 必须 try/catch + 10MB 上限。
7. **SW `skipWaiting` 仅由用户点击触发**，禁止自动激活；dev 不注册 SW。
8. `output: "export"` 约束：禁 API Route/服务端渲染。

---

## 9. 已知边界与行为备注

> 本章诚实记录「实现现状如此」的边界行为，分两类：**9.1 语义边界**（测试锁定或产品已接受的现状，改动需明示决策）；**9.2 已确认缺陷/文案不一致**（待修复决策）。改动任何一条前需更新对应测试。

### 9.1 语义边界（现状锁定）

| # | 行为 | 说明 |
| --- | --- | --- |
| 1 | `inferTodoType` 为纯子串匹配 | 「术后复查不是术前安排」→「开术前」（测试锁定）；无语义判断 |
| 2 | 「明天出院」类型永不产出 | 枚举存在但推断只产「出院」（日期仍正确解析） |
| 3 | 待办页 toggle 为页面内联实现 | 与 `lib` 的 `toggleTodo` 两处代码，行为等价；改完成逻辑需同步两处 |
| 4 | SwipeableTodo 停靠式交互为备用实现 | 当前无调用方，组件 API 保留 |
| 5 | 待办排序逻辑两处重复 | 详情页与 TodoListView 各一份，改动需同步 |
| 6 | `expandCompleted` 仅挂载时生效 | 待办页切到「已完成」筛选若组件未重挂载，已完成项需手动展开 |
| 7 | 关闭「换药计划」开关不清除已有病人级计划 | autoSave 只在开关开启时处理该字段；与开关文案存在语义落差 |
| 8 | 设置页换药规则无 `maxDay > earlyInterval` 校验 | 仅要求整数 ≥1；病人表单校验更严 |
| 9 | 「换药计划已完成(已超14天)」文案硬编码 14 | 非动态读取 maxDay |
| 10 | 基础规则下 computeBedType 与 resolveOrder 的已知分裂 | 同病区重复 bedBase（如 `309W01` 与子床号 `309W01-1`）：床型判定都算 real，查房入列只放一人（parity 测试仅记录不断言） |
| 11 | 基础规则床型匹配不校验病区 | `309W01` 与 `310W01` 在基础规则下都判 real（与「多病区套用同套块」语义一致） |
| 12 | settings 未加载时全部床判虚拟 | `filterHomeRows` 防御语义：此时若开启隐藏开关首页会短暂清空（测试锁定） |
| 13 | `parseTime` 不拦截 JS 日期进位 | 如「2月31日」按 3 月 3 日计 |
| 14 | 撤销基于对象引用快照 | 撤销窗口内再次编辑同记录，撤销恢复的是删除前快照 |
| 15 | 导入要求 `diagnosis` 非空 | 旧导出文件若含空诊断病人，重新导入将整体失败（自兼容陷阱） |
| 16 | pinyin-pro 为死依赖 | 源码无任何 import；**当前实现不存在拼音排序/分组**（唯一中文排序是病区的 localeCompare("zh")） |
| 17 | 「查血」快捷按钮不去重 | 「换药」与快捷待办按各自口径查重（§4.3.4/§4.3.5） |
| 18 | 添加病人/批量导入弹层状态在关闭后保留 | Tab 选择与导入文本不重置（组件常驻） |
| 19 | `version.json` 双重排除 | 不预缓存 + 前端 no-cache，是更新检测成立的前提 |
| 20 | `blockLabel` 连续判定只看末位数字 | `309W09 + 309WJ10` 数字不连续则全列展示 |

### 9.2 已确认缺陷与文案不一致（待修复决策）

| # | 位置 | 问题 | 影响 |
| --- | --- | --- | --- |
| 1 | 首页操作菜单「编辑 / 更改分组」（`app/page.tsx:446-449`） | 同一事件内 `setEditOpen(true)` 与 `setMenuPatient(null)` 批处理后表单收到 `patient=null` | **打开的是空白新增表单而非编辑该病人**；详情页编辑入口正常 |
| 2 | 首页删除病人确认框（`app/page.tsx:470`） | 文案「可在 5 秒内撤销」，实际 toast 默认 3 秒 | 文案与实际撤销窗口不一致 |
| 3 | 批量导入删除确认框 | 文案「级联删除其未完成待办」，实际删除该病人**全部**待办（含已完成） | 文案低估删除范围 |

---

## 10. 附录：关键常量速查

| 常量 | 值 | 位置/说明 |
| --- | --- | --- |
| 应用版本 | 2.17.2 | package.json / version.json / sw.js 构建同步 |
| 数据库 | `ClinicalDB` v1，三表 | lib/db.ts |
| 默认床号模板 | `^(\d{3})([A-Z])([A-Z]{0,2})?(\d{2})$` | 4 捕获组：病区基底/方位/标记/床号 |
| 默认特殊标记 | `["J", "YZ"]` | 需大写存储 |
| 默认换药规则 | `{2, 3, 14}` → 第 2/5/8/11/14 天 | 全局默认，病人可覆盖 |
| 默认查房配置 | 18 病房块 + 15 加床块（309W，跳号 J05/J11） | §4.9.4 |
| 默认分组 | 解组/勇组/李组/王组 | 默认分组色 `#e2e8f0` |
| 默认快捷待办 | 换药/查血/术前准备 | §4.11 |
| 导入文件上限 | 10MB | 读取前拒绝 |
| 基础规则向导 | 初值 40 床 / 每房 3 床；钳制 1–2000 / 1–200 | §4.9.5 |
| 首页虚拟化 | > 50 行（行高 68，overscan 10） | §4.2.7 |
| 待办虚拟化 | > 30 条（行高 56，overscan 8） | §4.4.5 |
| 长按菜单 | 500ms | 病人卡片 |
| 滑动完成阈值 | ±56px（约束 ±90，elastic 0.08） | 待办卡片 |
| 编辑自动保存 | 400ms 防抖 + onBlur；「已自动保存」1.5s 淡出 | §4.1.2 |
| Toast 默认时长 | 3000ms | action 与关闭绑定 |
| 更新状态回弹 | 3500ms（latest/error → idle） | §4.16 |
| SW 轮询 | 60s + 回前台 | §4.16 |
| 导航角标 | pending 且 today/overdue；>99 显示 99+ | §4.7.4 |
| 换药幂等键 | `{patientId}\|换药\|{dueDate}` | §4.8.5 |
| 到期标签兜底 | 无日期排序键 `9999-99-99` | 待办排序 |
| 床型匹配哨兵 | bedBase 缺失按 `-1` 比较 | computeBedType |
| 新建加床块伪选项 | `__new_extra__` | 床号识别页 |
| 分组色板 | 8 色，初始第 3 色 `#bbf7d0` | 分组管理页 |
| 动效曲线 | `[0.22, 1, 0.36, 1]`；spring 400/30；sheet 300/34 | 仅 transform/opacity |
| sessionStorage | `cc:pid` / `homeScroll` | §5.5 |
| localStorage | `cc-dev-sw`（仅开关） | §5.5 |

---

> **维护约定**：本 PRD 以代码为实现基线。修改功能时同步更新本文档与对应测试；修改 `resolveOrder` / `bed-type` / 导入导出校验等核心逻辑必须附单元测试（红线 §8.3）。UI 视觉规范不在本文档维护。
